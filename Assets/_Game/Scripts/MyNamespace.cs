namespace MyNamespace
{
    public enum Direct { Forward, Back, Right, Left }
    public enum GameState { Start, Playing, Win, Lose,End}
    public enum TitleType{Wall, Road, Bridge, WinPos, Corner}
}
